{
  "name": "track-slide",
  "version": "2.0.0",
  "description": "Constructor to slide multiple objects on a track rather than individually.",
  "main": "track-slide.js",
  "scripts": {
    "standalone": "browserify track-slide.js --outfile standalone/track-slide.js --standalone TrackSlide --exclude jquery",
    "dist": "uglifyjs standalone/track-slide.js --output standalone/track-slide.min.js --comments",
    "build": "npm run standalone && npm run dist"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@stash.c2mpg.com:7999/c2/track-slide.git"
  },
  "keywords": [
    "slider"
  ],
  "author": "The C2 Group",
  "license": "MIT",
  "dependencies": {
    "bloody-debounce-af": "^1.0.0",
    "c2-event-handler": "^2.3.1",
    "jquery": "^2.1.4",
    "jquery-dragger": "^1.5.0"
  }
}
